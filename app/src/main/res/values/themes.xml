<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.GetTaskDone"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Use white icons in the status bar -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Use a light navigation bar on supported Android versions -->
        <item
            name="android:windowLightNavigationBar"
            tools:targetApi="27">true</item>

    </style>

    <style name="Theme.GetTaskDone"
        parent="Base.Theme.GetTaskDone" />

</resources>